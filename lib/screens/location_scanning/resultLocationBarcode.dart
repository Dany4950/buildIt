import 'package:flutter/material.dart';

class scan_location_Items_result extends StatelessWidget {
  const scan_location_Items_result({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(),
    );
  }
}